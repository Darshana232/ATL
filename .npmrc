# Refuse to install if the local Node version does not satisfy package.json "engines".
# Better to fail at install time than to hit a mysterious runtime error later.
engine-strict=true

# Pin exact versions instead of "^1.2.3" ranges.
# Reproducible installs: everyone (and CI) gets byte-identical dependencies.
save-exact=true

# Less console noise.
fund=false
